#include<bits/stdc++.h>
using namespace std;
void stringtokeys(vector<string>keys){
    int n= keys.size();
    vector<vector<string>> arr(n);
    for(int i = 0;i<keys.size();i++){
        stringstream ss(keys[i]);
        string token;
        while(ss>>keys[i]){
           
        }
    }
}
int main(){
    vector<string> words = {"hello word","who are you"};
}